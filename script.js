const text='Graphic Designer.';const typing=document.getElementById('typing');let i=0;function type(){if(i<text.length){typing.textContent+=text.charAt(i);i++;setTimeout(type,95)}}type();const navbar=document.querySelector('.navbar');window.addEventListener('scroll',()=>{navbar.classList.toggle('scrolled',window.scrollY>50);reveal()});function reveal(){document.querySelectorAll('.reveal').forEach(el=>{if(el.getBoundingClientRect().top<window.innerHeight-120){el.classList.add('active');el.querySelectorAll('.skill b').forEach(bar=>{bar.style.width=bar.dataset.width})}})}reveal();
